server:
  port: 23001
spring:
  application:
    name: patientservice
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      #        namespace: xx
      #        cluster-name: GLA
      config:
        server-addr: localhost:8848
        file-extension: yaml
